import React, { useState } from 'react'
import axios from 'axios'
import { View, Text, Image, Button, TouchableOpacity } from 'react-native'
import { useUser } from '../API/AuthService.js'
const List = () => {
  return (
    <View>
      <Text>My Unique List</Text>
    </View>
  )
}

export default List
